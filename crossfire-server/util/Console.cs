using System;

namespace crossfire_server.util
{ /*
    public class Console
    {
        public static void Log(string information)
        {
            System.Console.Out.WriteLine(information);
        }

        public static void Log(string information, Exception e)
        {
            System.Console.Out.WriteLine("%s | %s%n", information, e.Message);
        }

        public static void PrintBytes(byte[] array, String name) {
            for (int k = 0; k < array.Length; k++) {
                Log(name + "[" + k + "] = " + "0x" +
                    UnicodeFormatter.byteToHex(array[k]));
            }
        }
    } */
}